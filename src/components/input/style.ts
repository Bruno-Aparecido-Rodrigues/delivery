import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
    label: {
        color: '#FFF',
        fontSize: 14,
        fontWeight: '600',
        letterSpacing: 0.5,
    },
    input: {
        backgroundColor: '#555',
        borderRadius: 8,
        color: '#FFF',
        fontSize: 16,
        paddingHorizontal: 16,
        paddingVertical: 12,
        borderWidth: 1,
        borderColor: '#666',
    },
    wrapper: {
        gap: 6,
    },
});
